class MapNpcId:
    def __init__(self, mapnpcid, dialog, effect, effectdelay, isdisabled, ismoving, issitting, mapid, mapx, mapy, name, npcvnum, position, say, delay):
        self.mapnpcid = mapnpcid
        self.dialog = dialog
        self.effect = effect
        self.effectdelay = effectdelay
        self.isdisabled = isdisabled
        self.issitting = issitting
        self.mapid = mapid
        self.mapx = mapx
        self.mapy = mapy
        self.name = name
        self.npcvnum = npcvnum
        self.position = position
        self.say = say
        self.delay = delay

        #TODO Getter Setter